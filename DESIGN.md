# Tray popup

Operate mode. Inherit CodeNotch's compact instrument style, adapted to a Windows Acrylic tray flyout as the user requested. The Windows compositor supplies native Acrylic; a translucent content layer preserves readability. Light and dark follow the system. One scrollable provider list, detailed allowance bars, small provider-colored rings, clearly labelled used percentages and reset countdowns. Segoe UI follows Windows typography. Teal is the main accent; amber means stale or near limit; red means exhausted or failed. Status words accompany color. Do not sum unrelated provider allowances.

The window opens from the tray, remains inside the active monitor work area, closes on Escape or blur, and never appears as a taskbar app button. Right click provides Refresh, Start with Windows, and Quit. Missing providers remain discoverable with sign-in guidance. No fabricated live values; browser-only preview has an explicit sample-data banner.

